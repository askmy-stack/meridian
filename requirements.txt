# Core dependencies
six==1.16.0
kafka-python==2.0.2
pydantic==2.5.0
structlog==23.2.0
python-dotenv==1.0.0

# API Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Data processing
pandas==2.1.3
requests==2.31.0
aiohttp==3.9.1

# GDELT specific
gdelt==0.1.14

# Neo4j
neo4j==5.15.0

# PostgreSQL/TimescaleDB
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Vector DB
qdrant-client==1.7.0

# ML/Analysis
numpy==1.26.2
scikit-learn==1.3.2
hmmlearn==0.3.2

# Phase 3: Intelligence Engine
# BERT Event Classifier
torch==2.1.2
transformers==4.36.2
sentencepiece==0.1.99

# XGBoost Risk Scorer
xgboost==2.0.3
shap==0.44.1

# spaCy NER
spacy==3.7.2

# Weak Signal Detection (LSTM)
tensorflow==2.15.0

# Entity Resolution
rapidfuzz==3.6.1

# API Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # pin: passlib 1.7.4 breaks on bcrypt 4.1+ / Python 3.13

# MLflow tracking (Phase 3 model registry)
mlflow==2.9.2

# Phase 4 (heavy graph deps — install on demand, comment out by default)
# torch-geometric==2.4.0
# torch-geometric-temporal==0.54.0

# Causal inference (Phase 4 sensitivity analysis)
# Causal inference (optional — association fallback when not installed)
# dowhy==0.11.1
# econml==0.14.1

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
httpx==0.26.0  # for FastAPI TestClient async tests

# Type checking
mypy==1.7.1

# Linting
black==23.11.0
ruff==0.1.6
