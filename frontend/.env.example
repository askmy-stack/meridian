# Frontend environment example.
# Copy to .env.local for development overrides (gitignored).

# Base URL for API calls. Use /api in dev to leverage Vite proxy.
VITE_API_BASE_URL=/api

# Optional: Mapbox token for risk-map tile rendering
VITE_MAPBOX_TOKEN=
